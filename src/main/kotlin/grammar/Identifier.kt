package grammar

// 식별자

fun main() {
    identifier()
}

private fun identifier() {
    val _case1 = 0
    val case2 = 0
    val `name with spaces` = 1
    println("`name with spaces` = $`name with spaces`")
}